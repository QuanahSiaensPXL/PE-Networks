# Kopieer naar .env en vul in. .env zelf staat in .gitignore.
# De key matcht de credential_ref in de devices-tabel.
# Credentials voor de fysieke ISR4221 (R1-HW)
R1HW_USERNAME=admin
R1HW_PASSWORD=changeme
R1_NETCONF_USERNAME=admin
R1_NETCONF_PASSWORD=changeme
